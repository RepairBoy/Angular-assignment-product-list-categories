import { BgColorandHoverDirective } from './bg-colorand-hover.directive';

describe('BgColorandHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BgColorandHoverDirective();
    expect(directive).toBeTruthy();
  });
});
